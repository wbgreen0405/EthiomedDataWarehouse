# packages.yml
packages:
  - package: dbt-labs/dbt_utils
    version: 1.1.1 # This is a common version for dbt 1.9.x.
                   # You can check for the very latest compatible version on dbt Hub:
                   # https://hub.getdbt.com/dbt-labs/dbt_utils/latest/